import React from 'react';

// In a real data table, this would be column definitions
// For this structure, we'll keep it as a placeholder to satisfy the file requirement
export const columns = [
  { header: "Patient", accessor: "patientName" },
  { header: "Date", accessor: "date" },
  { header: "Status", accessor: "status" },
  { header: "Doctor", accessor: "doctorId" },
];